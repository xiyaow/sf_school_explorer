// config.template.js — committed placeholder, safe to check into git.
//
// The GitHub Actions workflow (.github/workflows/deploy-pages.yml) copies
// this file to config.js at deploy time, substituting the real key from
// your repo's GOOGLE_MAPS_API_KEY secret. Locally, just copy this file to
// config.js yourself and paste your key in for testing (config.js is
// gitignored, so it won't get committed).

const GOOGLE_MAPS_API_KEY = "__GOOGLE_MAPS_API_KEY__";
