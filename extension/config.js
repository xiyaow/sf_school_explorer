// config.js — the one file you edit after deploying map_site/ to GitHub Pages.
//
// 1. Push the map_site/ folder to a GitHub repo (see map_site/README.md).
// 2. Enable GitHub Pages for that repo (Settings -> Pages -> deploy from
//    main branch / root or /docs, whichever you used).
// 3. Copy the resulting URL + "/map.html" and paste it below.
//
// Leave as-is (with the placeholder) and the popup will just skip the
// map embed and show the text results only.

const MAP_SITE_URL = "https://YOUR_GITHUB_USERNAME.github.io/sf-school-map/map.html";
